# Same-origin API routing is recommended in Vercel previews and production.
VITE_SERVER_URL=/api
# Only the publishable key may be exposed to the browser.
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_replace_me
